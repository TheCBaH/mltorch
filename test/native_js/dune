; Stage 7's own mirrored-Kernel boundary tests (tail-call plan; see .ai/):
; links [native_js], not [native] -- test/native/dune's own inline tests run
; under jsoo too (`modes best js`), but that recompiles ORDINARY [native]
; to JS, never switching to this mirror, so nothing today exercises
; native_js's overridden `Kernel.Limits.Hard.eval_depth` (12288, vs.
; native's own 1536; see js/jsoo/native_js/kernel_hard.ml). `best` here is
; a bonus native/bytecode sanity run of the same boundaries against the
; mirror's copy of the SHARED constants (`depth`, `eval_recursion`),
; unaffected by the override; `js` is the one that actually proves it.

(library
 (name native_js_test)
 (libraries core err_trace native_js)
 (inline_tests
  (modes best js))
 (preprocess
  (pps ppx_expect)))
