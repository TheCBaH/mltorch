; Pure PT2 -> native-graph importer.  This deliberately depends on neither
; [aten] nor [interp]: archive payloads are represented by native input ids and
; resolved later through Pt2_native_graph.captured_targets.

(library
 (name native_interp)
 (wrapped false)
 (libraries err_trace native native_graph pt2 pytorch_types schema_runtime))
