; Native-operation random walk: drives each native op's own config space
; (its Walk submodule in lib/native/ops) and verifies the Direct and Symbolic
; backends agree. Pure OCaml (no libtorch); the verify oracle is self-contained.

(library
 (name native_op_walk)
 (libraries err_trace native walk_core))
