(* Phantom role markers for index expressions, defined in [Core.Role] (see
   there for why they cannot be local to this library). *)

module Position = Core.Role.Position
module Delta = Core.Role.Delta
