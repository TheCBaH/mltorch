; ATen specialization of the shared walk runner: fixes [verify] to the
; ATen-vs-native harness (aten_spec_run). The generic loop lives in walk_core.

(library
 (name native_walk)
 (wrapped false)
 (libraries walk_core aten_spec aten_spec_run aten_walk_recipes))
