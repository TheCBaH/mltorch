; The jsoo route for Stage 0's evaluation-order oracle, over [expr_js] (tail-call
; conversion, Stage 3) so this exercises the JS-preprocessed evaluator. The
; checked-in expr.ml shim (`include Expr_js.Expr`) matches every other Expr
; consumer's pattern -- see .ai/.

(copy_files
 (files %{workspace_root}/js/probe/order_probe.ml))

(executable
 (name order_probe)
 (modes js)
 (modules order_probe expr)
 (libraries expr_js err_trace))
