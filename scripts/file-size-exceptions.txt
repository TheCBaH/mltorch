# Exception list for scripts/check-file-size.sh.
#
# Format: path|review-by|reason
#   - path is repo-relative, matched exactly against the checked-out tree.
#   - review-by is a date; re-justify or remove the entry when it passes.
#
# A file listed here is exempt from both the 1000-line tree check and the
# 750-line new-file check. Splitting a listed file back under the threshold
# should remove its line here in the same change.
lib/native/ops/lstm.ml|2026-12-01|Landed as a single commit that consolidates the op's incremental development; under the 1000-line tree cap on its own.
lib/native4d/lower_engine.ml|2026-12-01|Formatting expanded this established lowering module; split its op families before this review date.
lib/native_aten_bridge/op_bridge_shape.ml|2026-12-01|Formatting expanded this established bridge module; split its shape families before this review date.
lib/native_interp/native_interp_lower_shape.ml|2026-12-01|Formatting expanded this established importer module; split its shape families before this review date.
